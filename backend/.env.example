# SQLite (local development, zero setup)
DATABASE_URL="file:./dev.db"
# For production PostgreSQL, switch provider in prisma/schema.prisma and use:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/riverside_hms?sslmode=require"
JWT_SECRET="replace-with-at-least-32-random-characters"
CLIENT_ORIGIN="http://localhost:5173"
PORT=3000
